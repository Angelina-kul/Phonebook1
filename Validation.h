#include <string>
using namespace std;

void probeli(string& s);

bool valid_Name(const string& name);
bool valid_Phone(const string& phone); 
bool valid_Email(const string& email);
bool valid_Bday(const string& dateStr);